"""Phase 0 ingestion — builds the knowledge base from public data.

Everything here is OFF by default (see config feature flags) so the production
software runs without ever touching external sites. Switch it on later to
"train" the system on the council website and published FOI responses.
"""
