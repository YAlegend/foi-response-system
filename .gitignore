__pycache__/
*.pyc
.venv/
venv/
*.db
.env
# Real env files (may hold SMTP creds); keep the *.example templates tracked.
.env.production
.env.local
.pytest_cache/
.DS_Store
# Local DB backups (e.g. foi.db.bak made before re-seeding)
*.bak
# Claude Code local/personal artifacts (not shared)
.claude/settings.local.json
.claude/*.lock
# Vendored semantic embedding model (large ONNX binary) — fetched via
# `python -m app.fetch_model`; ship it with the deploy artifact, not git.
models/
